<%= form_for(album, html: {class: 'model-form'}) do |f| %>
  <% if album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(album.errors.count, "error") %> prohibited this album from being saved:</h2>

      <ul>
      <% album.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label> Album Name</label>
    <%= f.text_field :name, class: "form-control", placeholder: "Album Name"  %>
  </div>
  <div class="field">
    <%= f.label :cover_photo %>
    <%= f.file_field :cover_photo, class: "form-control", placeholder: "Select Cover Photo for Album" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
